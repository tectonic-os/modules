description "GameMode, the daemon a game asks for the machine's full attention through"

supports "fedora" "debian" "ubuntu"

packages {
    fedora "gamemode"
    debian "gamemode"
    ubuntu "gamemode"
}

description "The just command runner and the system justfile every module's recipes are collected into"

supports "fedora" "debian" "ubuntu"

provides "just"

collects "justfile.inc" into="/usr/share/just/justfile.apps" priority=500

packages "just"

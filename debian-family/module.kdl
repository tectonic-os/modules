description "The Debian base family: the build environment every layer runs in, and the root filesystem work that closes it"

supports "debian" "ubuntu"

provides "build-environment"

helpers "lib/family.sh"

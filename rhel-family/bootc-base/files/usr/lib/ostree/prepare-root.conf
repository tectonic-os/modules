# What `centos-bootc:stream10` ships at this path, which is owned by no package
# there either: the image build writes it. `enabled = yes` rather than
# `verity`, since a sealed deployment is the image's decision and `layout`
# carries it.
[composefs]
enabled = yes

[sysroot]
readonly = true

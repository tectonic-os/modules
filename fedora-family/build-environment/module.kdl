description "dnf as the build environment every Fedora-family layer installs through, and the root filesystem work that closes it"

supports "fedora"

provides "build-environment"

helpers "lib/family.sh"
